Imports System.IO
Imports SteamDatabase
Imports ValveResourceFormat

Public Class ArtifactCard

    'Private Sub Artifact_Load(sender As Object, e As EventArgs) Handles MyBase.Load
    '    Dim VPK As New ValvePak.Package
    '    VPK.Read("D:\Program Files\Steam\steamapps\common\dota 2 beta\game\dota\pak01_dir.vpk")
    '    Dim f As String = "panorama\images\loadingscreens\artgerm_invoker\loadingscreen_tga.vtex_c"
    '    Dim b As Byte() = Nothing
    '    VPK.ReadEntry(VPK.FindEntry(f), b)
    '    'Dim fs As New FileStream($"file\{f.Split("/"c).Last}", FileMode.Create)
    '    'Dim bw As BinaryWriter = New BinaryWriter(fs)
    '    'bw.Write(b)
    '    'bw.Close()
    '    'fs.Close()
    '    VPK.Dispose()
    '    Dim r As New ValveResourceFormat.Resource
    '    Dim stm As Stream
    '    Dim bw As BinaryWriter = New BinaryWriter(stm)
    '    'r.Read(stm)
    'End Sub

End Class